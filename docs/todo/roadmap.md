# Polaris 功能路线图

本文档记录了项目可以扩展和优化的方向，按优先级分类。

---

## 目录

- [P0 - 核心功能补齐](#p0---核心功能补齐)
- [P1 - 用户体验提升](#p1---用户体验提升)
- [P2 - 高级功能](#p2---高级功能)
- [代码质量优化](#代码质量优化)
- [架构改进](#架构改进)

---

## P0 - 核心功能补齐

这些是 Claude Code CLI 已有但当前项目未实现的核心功能，建议优先实现。

### 1. 模型选择器

**描述**: 允许用户选择使用 Haiku、Sonnet 或 Opus 模型

**当前状态**: ❌ 未实现，默认使用 CLI 配置的模型

**实现方案**:
- 在设置面板添加模型选择组件 (`components/Settings/ModelSelector.tsx`)
- `configStore` 添加 `model` 字段，支持 `'haiku' | 'sonnet' | 'opus'`
- `chat.rs` 启动命令添加 `--model` 参数注入

**文件位置**:
- 新建: `src/components/Settings/ModelSelector.tsx`
- 修改: `src/stores/configStore.ts`
- 修改: `src-tauri/src/commands/chat.rs`

---

### 2. Token 统计面板

**描述**: 实时显示 Token 使用情况，包括本次对话和累计统计

**当前状态**: 🟡 StreamEvent 中有 usage 字段但未解析显示

**实现方案**:
- 在工具面板或聊天区域添加 Token 统计组件
- 解析 `StreamEvent` 中的 `usage` 数据
- 实时更新显示，支持累计统计

**文件位置**:
- 新建: `src/components/Chat/TokenUsage.tsx`
- 修改: `src/types/chat.ts` (完善 Usage 类型定义)
- 修改: `src/stores/chatStore.ts` (添加 token 统计状态)

---

### 3. 历史会话管理

**描述**: 管理历史对话记录，支持查看、恢复、删除

**当前状态**: 🟡 支持会话恢复，但没有历史列表

**实现方案**:
- 创建会话历史面板组件
- 使用 localStorage 持久化会话元数据
- 支持会话重命名、删除、恢复操作

**文件位置**:
- 新建: `src/components/Chat/SessionHistory.tsx`
- 新建: `src/stores/sessionStore.ts`
- 修改: `src/components/Chat/ChatMessages.tsx` (添加历史入口)

---

### 4. 编辑器标签页

**描述**: 支持同时打开多个文件，通过标签页切换

**当前状态**: ❌ 单文件编辑器

**实现方案**:
- 扩展 `fileEditorStore` 支持多文件状态
- 创建标签页组件，支持切换和关闭
- 保持编辑器状态不丢失

**文件位置**:
- 新建: `src/components/Editor/EditorTabs.tsx`
- 修改: `src/stores/fileEditorStore.ts`
- 修改: `src/components/Editor/EditorPanel.tsx`

---

### 5. Diff 预览组件

**描述**: 可视化显示文件变更差异

**当前状态**: ❌ 未实现

**实现方案**:
- 使用 `react-diff-viewer` 或自定义实现
- 集成到工具面板，当 AI 修改文件时显示 diff
- 支持 inline 和 split 两种视图模式

**文件位置**:
- 新建: `src/components/Editor/DiffViewer.tsx`
- 新建: `src/components/Editor/DiffPanel.tsx`
- 依赖: `npm install react-diff-viewer-continued`

---

### 6. 上下文管理面板

**描述**: 显示当前对话加载的所有文件，支持管理上下文

**当前状态**: ❌ 未实现

**实现方案**:
- 显示当前会话引用的所有文件列表
- 显示每个文件占用的 Token 估算
- 支持手动移除不需要的文件

**文件位置**:
- 新建: `src/components/Chat/ContextPanel.tsx`
- 新建: `src/stores/contextStore.ts`
- 修改: `src/stores/chatStore.ts` (记录文件引用)

---

## P1 - 用户体验提升

这些功能能显著提升用户体验，建议在核心功能完成后实现。

### 7. 快捷键系统

**描述**: 完整的快捷键绑定和自定义

**当前状态**: 🟡 基本快捷键（Enter 发送等），但无配置界面

**实现方案**:
- 创建快捷键设置面板
- 支持自定义绑定
- 添加命令面板 (Cmd+P 风格)

**文件位置**:
- 新建: `src/components/Settings/HotkeySettings.tsx`
- 新建: `src/hooks/useHotkey.ts`
- 新建: `src/components/Common/CommandPalette.tsx`

---

### 8. 提示词模板系统

**描述**: 保存常用的提示词模板，快速调用

**当前状态**: ❌ 未实现

**实现方案**:
- 创建模板管理组件
- 支持保存、编辑、删除模板
- 输入框 `/` 触发时显示模板列表

**文件位置**:
- 新建: `src/components/Chat/TemplateSelector.tsx`
- 新建: `src/stores/templateStore.ts`
- 修改: `src/components/Chat/ChatInput.tsx`

---

### 9. 搜索增强

**描述**: 支持代码内容搜索，不仅限于文件名

**当前状态**: 🟡 仅支持文件名搜索

**实现方案**:
- 扩展 `search_files` Rust 命令支持内容搜索
- 添加搜索类型切换（文件名/内容）
- 显示搜索结果上下文预览

**文件位置**:
- 修改: `src-tauri/src/commands/file_explorer.rs`
- 修改: `src/hooks/useFileSearch.ts`
- 修改: `src/components/FileExplorer/SearchBar.tsx`

---

### 10. 主题系统

**描述**: 支持多套内置主题和自定义主题

**当前状态**: 🟡 仅支持深色主题

**实现方案**:
- 定义主题变量系统
- 创建主题选择器
- 支持自定义主题编辑器

**文件位置**:
- 新建: `src/components/Settings/ThemeSelector.tsx`
- 修改: `src/index.css` (使用 CSS 变量)
- 新建: `src/stores/themeStore.ts`

---

### 11. 自动保存

**描述**: 编辑器内容自动保存，防止意外丢失

**当前状态**: ❌ 未实现

**实现方案**:
- 编辑器内容变化时自动保存到临时存储
- 应用重启后恢复编辑状态
- 支持禁用自动保存

**文件位置**:
- 新建: `src/services/autoSave.ts`
- 修改: `src/components/Editor/Editor.tsx`

---

## P2 - 高级功能

这些是增强型功能，可作为差异化竞争优势。

### 12. Git 可视化

**描述**: 图形化显示 Git 提交历史和分支关系

**实现方案**:
- 使用 `gitgraph.js` 或自定义绘制
- 显示分支图、提交时间线
- 支持合并冲突可视化

**文件位置**:
- 新建: `src/components/Git/GitGraph.tsx`
- 新建: `src/components/Git/CommitTimeline.tsx`
- 依赖: `npm install @gitgraph/react`

---

### 13. 文件依赖图

**描述**: 可视化显示项目文件的导入依赖关系

**实现方案**:
- 静态分析 import/require 关系
- 使用图形库渲染依赖图
- 支持交互式浏览

**文件位置**:
- 新建: `src/components/Analysis/DependencyGraph.tsx`
- 新建: `src/services/dependencyAnalyzer.ts`
- 依赖: `npm install reactflow`

---

### 14. 智能补全

**描述**: 基于对话历史和上下文的智能输入建议

**实现方案**:
- 分析用户常用命令和操作
- 预测用户意图并提供建议
- 上下文感知的补全

**文件位置**:
- 新建: `src/services/suggestionEngine.ts`
- 修改: `src/components/Chat/ChatInput.tsx`

---

### 15. 对话分享

**描述**: 生成可分享的对话链接

**实现方案**:
- 导出为 HTML 格式
- 上传到临时服务器或本地生成
- 支持过期时间设置

**文件位置**:
- 新建: `src/services/chatShare.ts`
- 修改: `src/components/Chat/ChatMessages.tsx`

---

### 16. 协作标注

**描述**: 在 AI 回复中添加标注和评论

**实现方案**:
- 支持选中文本添加标注
- 标注持久化存储
- 导出时包含标注内容

**文件位置**:
- 新建: `src/components/Chat/AnnotationPanel.tsx`
- 修改: `src/components/Chat/MessageBubble.tsx`

---

### 17. 批量操作

**描述**: 支持批量处理文件

**实现方案**:
- 文件浏览器多选模式
- 批量编辑、删除、移动
- 进度显示

**文件位置**:
- 新建: `src/services/batchOperations.ts`
- 修改: `src/components/FileExplorer/FileTree.tsx`

---

### 18. 宏命令

**描述**: 录制常用操作序列，一键执行

**实现方案**:
- 操作录制功能
- 宏命令管理
- 参数化宏命令

**文件位置**:
- 新建: `src/services/macroRecorder.ts`
- 新建: `src/components/Chat/MacroPanel.tsx`

---

## 代码质量优化

这些是代码层面的优化，不影响功能但提升代码质量。

### 19. 清理未使用依赖

**描述**: 移除未使用的 npm 包

**当前状态**: `react-markdown`, `rehype-highlight`, `remark-gfmt` 已安装但未使用

**执行命令**:
```bash
npm uninstall react-markdown rehype-highlight remark-gfm
```

**影响**: 减少约 200KB 打包体积

---

### 20. 改进 TypeScript 类型

**描述**: 替换 `any` 类型为具体类型

**位置**:
- `src/utils/cache.ts:231-232` - `any[]` → `FileMatch[]` / `Command[]`
- `src/hooks/useDebounce.ts:23,35` - 改进泛型约束
- `src/types/chat.ts:70` - `usage?: unknown` → 定义具体接口

---

### 21. 输入验证

**描述**: 添加用户输入长度限制

**位置**: `src/components/Chat/ChatInput.tsx`

**实现**: 添加 `MAX_INPUT_LENGTH` 常量和验证逻辑

---

### 22. 统一错误处理

**描述**: 建立统一的错误处理机制

**实现方案**:
- 创建全局错误边界
- 统一错误提示组件
- 错误日志上报

**文件位置**:
- 新建: `src/components/Common/ErrorHandler.tsx`
- 新建: `src/services/errorReporting.ts`

---

### 23. 添加单元测试

**描述**: 为核心功能添加测试

**当前状态**: ❌ 无测试

**实现方案**:
- 使用 Vitest 添加单元测试
- 为 stores、hooks、services 添加测试
- 设置 CI 自动运行测试

**文件位置**:
- 新建: `tests/` 目录
- 依赖: `npm install -D vitest @testing-library/react`

---

### 24. 添加 ESLint 和 Prettier

**描述**: 统一代码风格和质量检查

**当前状态**: ❌ 无配置

**实现方案**:
- 添加 `.eslintrc.cjs` 配置
- 添加 `.prettierrc` 配置
- 添加格式化脚本

**文件位置**:
- 新建: `.eslintrc.cjs`
- 新建: `.prettierrc`
- 依赖: `npm install -D eslint prettier`

---

### 25. 抽取重复代码

**描述**: Editor 组件中的扩展配置代码重复

**位置**: `src/components/Editor/Editor.tsx:151-177, 207-234`

**实现**: 抽取为 `createEditorExtensions()` 配置函数

---

## 架构改进

这些是架构层面的长期改进。

### 26. 插件系统

**描述**: 支持第三方扩展功能

**接口设计**:
```typescript
interface Plugin {
  name: string;
  version: string;
  commands?: Command[];
  onMessage?(message: Message): void;
  onToolCall?(tool: ToolCall): void;
}
```

---

### 27. 事件总线

**描述**: 组件间解耦通信

**实现方案**:
- 创建事件发射器
- 支持订阅/发布模式
- 类型安全的事件定义

---

### 28. 服务层抽象

**描述**: 将 CLI 调用抽象为服务接口

**目标**:
- 支持未来 API 直连
- 便于切换不同后端
- 统一错误处理

---

### 29. 性能优化

**描述**: 优化大文件和长列表性能

**优化点**:
- 虚拟滚动 (已实现 react-virtuoso)
- 大文件分块加载
- 懒加载组件
- 缓存策略优化

---

### 30. 离线支持

**描述**: 部分功能支持离线使用

**实现方案**:
- 本地缓存对话历史
- 离线时查看历史记录
- 网络恢复后同步

---

## 进度跟踪

使用以下表格跟踪整体进度：

| 类别 | 总数 | 未开始 | 进行中 | 已完成 |
|------|------|--------|--------|--------|
| P0 核心功能 | 6 | 6 | 0 | 0 |
| P1 用户体验 | 5 | 5 | 0 | 0 |
| P2 高级功能 | 7 | 7 | 0 | 0 |
| 代码质量 | 7 | 7 | 0 | 0 |
| 架构改进 | 5 | 5 | 0 | 0 |
| **合计** | **30** | **30** | **0** | **0** |

---

> 最后更新: 2026-01-11
> 维护者: Polaris Team
